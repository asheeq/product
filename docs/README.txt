Installation
Required: NodeJS >= 10.15.3

1. npm install -g sequelize-cli
2. cd into project root
3. npm install
4. npm install sequelize-cli
5. sequelize-cli db:create
6. sequelize-cli db:migrate
7. sequelize-cli db:seed:all
8. node ./bin/www (for windows) || npm start (for linux)
